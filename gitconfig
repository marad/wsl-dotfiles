[user]
    email = moriturius@gmail.com
    name = Marcin Radoszewski

[alias]
    graph = log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(bold yellow)%d%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
    tree = !"git graph"
    st = status
    ci = commit
    co = checkout
    br = branch
    aa = add --all
    di = diff
    dc = diff --cached  
[color]
    ui = true
    status = auto
    branch = auto
    interactive = auto
    diff = auto

[push]
    default = simple
[core]
    editor = vim
    autocrlf = input
    excludesfile = ~/.gitignore_global

[branch]
    autosetuprebase = always
[credential "https://github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
